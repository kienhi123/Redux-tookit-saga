import * as React from 'react';

export interface NotFoundProps { }

// eslint-disable-next-line @typescript-eslint/no-redeclare
export function NotFoundProps(props: NotFoundProps) {
    return (
        <div>
            NotFound
        </div>
    );
}
