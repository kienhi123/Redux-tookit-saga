import * as React from 'react';

export interface LoginPagesProps {
}

export default function LoginPages(props: LoginPagesProps) {
    return (
        <div>
            LoginPagesProps
        </div>
    );
}
